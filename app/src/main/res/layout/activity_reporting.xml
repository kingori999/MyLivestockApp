<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp">

    <Button
        android:id="@+id/generateLivestockReportButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Generate Livestock Report" />

    <Button
        android:id="@+id/generateNutritionReportButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Generate Nutrition Report" />

    <Button
        android:id="@+id/generateHealthReportButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Generate Health Report" />
</LinearLayout>
